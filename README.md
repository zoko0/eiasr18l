Project for studies, course Image and speech recognition(EIASR)
Topic: Face detection using histogram of gradients

To run:
1. Install python and pip
2. Run 'pip install -r requirements.txt'
3. Go to bin/ directory and run 'python run.py'

There is possible you must install tkinter additionally.